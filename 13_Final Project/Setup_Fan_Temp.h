void Setup_Temp_Fan(void);              // Prototype for function to setup temp fan
void Setup_Fan_Screen(void);            // Prototype for function to modify temp
void Initialize_Setup_Fan_Screen(void); // Prototype for function to initialize LCD screen for Setup Fan
void Update_Setup_Fan_Screen(void);     // Prototype for function update values in Setup Fan


#define setup_fan_x          15         // X location on LCD for setup fan label
#define setup_fan_y          60         // Y location on LCD for setup fan label
#define setup_data_fan_x     40         // X location on LCD for setup fan data
#define setup_data_fan_y     74         // Y location on LCD for setup fan data